services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: erp
      POSTGRES_PASSWORD: erp_dev_password
      POSTGRES_DB: erp_fulfillment
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U erp -d erp_fulfillment"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://erp:erp_dev_password@postgres:5432/erp_fulfillment
      NEXTAUTH_URL: http://localhost:3000
      # ต้องกำหนด NEXTAUTH_SECRET ผ่าน .env ก่อนรัน — ไม่มีค่า default เพื่อกันรันโดยไม่ได้ตั้งรหัสลับจริง
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:?กรุณากำหนดค่า NEXTAUTH_SECRET ใน .env ก่อนรัน (เช่น openssl rand -base64 32)}
    ports:
      - "3000:3000"

volumes:
  postgres_data:
